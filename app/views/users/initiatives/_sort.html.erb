<%= search_form_for [:users, @q], builder: SimpleForm::FormBuilder do |f| %>
  <%= f.input :state_in, collection: collection_user_states, input_html: { multiple: true } %>
  
  <%= f.button :submit, 'Фільтрувати' %>
<% end %>
