$('#initiatives').append("<%= j render 'users/initiatives/index', collection: @initiatives %>");
<% if @initiatives.next_page %>
  $('.pagination').replaceWith("<%= j paginate @initiatives %>");
<% end %>