<p><%= t('devise.mailer.password_change.greeting', recipient: @resource.email) %></p>

<p><%= t('devise.mailer.password_change.instruction') %></p>

<p><%= link_to t('devise.mailer.password_change.action'), edit_password_url(@resource, reset_password_token: @token) %></p>

<p><%= t('devise.mailer.password_change.instruction2') %></p>
<p><%= t('devise.mailer.password_change.instruction3') %></p>
