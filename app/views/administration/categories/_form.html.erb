<%= simple_form_for [:administration, @category] do |f| %>
  <%= f.input :title %>
  <%= f.input :position %>
  
  <%= f.button :submit, t("views.pages.global.button.#{action}"), class: 'btn btn-success btn-sm' %>
<% end %>