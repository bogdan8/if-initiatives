$('#initiatives').append("<%= j render 'administration/initiatives/index', collection: @initiatives %>");
<% if @initiatives.next_page %>
  $('.pagination').replaceWith("<%= j paginate @initiatives %>");
<% end %>