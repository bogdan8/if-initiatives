<%= provide :site_title, t('views.pages.users.index.title') %>
<%= provide :users_index, 'active' %>

<div class="container-fluid">
  <div class="row" id="users">
    <%= render 'administration/users/index', collection: @users %>
  </div>
  <div class="row">
    <div class="col-sm-12">
      <%= paginate @users %> 
    </div>
  </div>
</div>
