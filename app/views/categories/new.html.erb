<h2>Create category</h2>

<%= form_for @category, html: { multipart: true } do |f| %>
  <p>
    <%= f.label :title %>
    <%= f.text_field :title %>
  </p>

  <p>
    <%= f.label :position %>
    <%= f.number_field :position %>
  </p>

  <%= f.submit 'Submit' %>

<% end %>